func NewLocation(w, r) {
    reqData: {
        name: string
        age: uint
    }
    resData: {
        id:  int
        KitResData
    }
    reqBizSign = parseBizSign
    checkPremission()

    ```
    switch reqData.name {
        case Tmall, AmazonAlexa, GoogHome:
        default: 
            resData.Code = RequestParams
            resData.Msg = fmt.Sprintf(codemsg[RequestParams], "platform")
            return
    }

    ```
    AddLog()
}